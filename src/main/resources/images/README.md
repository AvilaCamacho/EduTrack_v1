Coloca aquí las imágenes usadas por la UI.

Archivos esperados:
- login1.png  -> Ilustración para la columna izquierda en Login.fxml. Recomendado: 1200x900 px (o 800x600 para menor tamaño), formato PNG o JPG.
- logo.png    -> Logo usado en el formulario. Recomendado: 120x120 px o un SVG para mejor escalado.

Notas:
- Añade los archivos en esta carpeta para que las rutas relativas en los FXML funcionen: por ejemplo `@../images/login1.png`.
- Optimiza las imágenes para web (exportar con compresión ligera) para evitar grandes paquetes en el JAR.